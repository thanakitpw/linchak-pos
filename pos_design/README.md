# pos_design — REFERENCE เท่านั้น

โฟลเดอร์นี้เป็น mockup ที่ export จาก Stitch **ไม่ใช่ซอร์สของแอป** ไม่เคยถูก import ไม่เคยถูก build

## ใช้ยังไง

- **ดู `screen.png`** — นั่นคือสิ่งที่ถูกอนุมัติ ใช้เป็นเป้าหมายตอนพอร์ต
- **อย่าลอก `code.html`** — เป็นขยะจาก export: มีคำขยะ (`flat`, `no`, `shadows`, `docked`, `full-width`) อยู่ในแอตทริบิวต์ `class`, `<link>` ซ้ำสองครั้งใน 26 จาก 28 ไฟล์, ช่องกรอกข้อมูล 50 ช่องเขียนด้วย class string ที่ต่างกัน 40 แบบ, และ class ที่ไม่เคย define อีกหลายสิบตัว
- **`verdant_growth/DESIGN.md` STALE** — สเกล radius ในนั้นเพี้ยนไป 1 ขั้นเต็มจากที่ mockup render จริง และ prose ระบุสีที่ไม่มีอยู่ในระบบ

## แทนที่ด้วย

`docs/design-system.md` — token, กฎ, ตารางแปลง class ทีละตัว, และสถานะพอร์ตทั้ง 28 หน้า

## เทียบภาพ

เปิด `/dev/tokens` แล้วเลือกชื่อ mockup ที่มุมขวาล่างเพื่อซ้อนภาพทับหน้าจอปัจจุบัน (เลื่อนปรับความทึบได้)
